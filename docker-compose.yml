services:
  rwlv-runtime:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    env_file:
      - .env
    volumes:
      - .:/app
      - ./generated:/app/generated
    stdin_open: true
    tty: true

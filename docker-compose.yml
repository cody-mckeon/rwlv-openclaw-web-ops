services:
  rwlv-runtime:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    env_file:
      - .env
    volumes:
      - .:/app
      - rwlv_generated:/app/generated
    stdin_open: true
    tty: true

volumes:
  rwlv_generated:
